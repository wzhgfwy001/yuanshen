# 小程序调试完成记录 (2026-04-10)

## 当前工作状态

### 云函数（已验证成功）

**querybenke/index.js 和 queryzhuanke/index.js:**

```javascript
// 完整查询逻辑：
// 1. 分数范围 + 省份筛选 + 选科筛选（正则匹配）
// 2. 原始查询（最多300条）
// 3. 如果 > 100条：排除 plan_count < 5
// 4. 如果还是 > 100条：排除 plan_count < 10
// 5. 如果还是 > 100条：按概率分组均匀采样凑够100条
// 6. 如果凑不够100条，放宽条件显示全部

// 概率分组均匀采样：
// - 冲(0-20%): 按分差从大到小采样
// - 稳中(20-40%): 按分差从大到小采样
// - 稳(40-60%): 按分差从小到大采样
// - 稳高(60-80%): 按分差从小到大采样
// - 保(80-100%): 按分差从小到大采样
```

### 前端（filter.js）- 查询参数
```javascript
var queryParams = {
  type: type,
  minScore: totalScore - 50,
  maxScore: totalScore + 30,
  predictedScore: totalScore
};
// subjectKeyword: 选科用|连接，如"物理|化学|生物"
// province: 省份筛选
```

## 已完成的功能
| 功能 | 状态 | 说明 |
|------|------|------|
| 分数范围查询 | ✅ | predicted_score_2025 |
| 省份筛选 | ✅ | province字段 |
| 选科筛选 | ✅ | major_category正则匹配 |
| 2025优先+2024补足 | ✅ | 2025不够用2024补 |
| 录取人数过滤 | ✅ | <5排除，<10排除，>100均匀采样 |
| <100条弹窗提示 | ✅ | confirmText/cancelText已修复为2字 |
| type参数传递 | ✅ | 区分本科/专科 |
| predictedScore | ✅ | 用于概率计算 |
| 本科选科默认逻辑 | ✅ | 没选=3门选科（不含不限） |
| 专科选科默认逻辑 | ✅ | 没选=3门+不限 |

## CloudBase查询语法
```javascript
// ✅ 正确写法
db.command.gte(min).and(db.command.lte(max))

// ❌ 错误写法
_.gte(min).and(_.lte(max))           // 链式错误
```

## 弹窗文字限制
```
微信showModal规定：confirmText/cancelText最多4个中文字符
```

## 调试踩坑记录
| 问题 | 修复 |
|------|------|
| result页面调用错误云函数 | 添加type参数 |
| showModal按钮文字超4字不显示 | 缩短为2字 |
| 多次where()条件覆盖 | 合并为单一查询对象 |

## 数据说明
- 选科筛选用major_category正则匹配（如"物理"匹配"/物理学类"）
- 录取人数过滤：先<5，再<10，最后均匀采样
- 选科默认逻辑：本科不含不限，专科含不限

## 下次继续
所有规则已实现完毕，无需继续调试。
如需修改：
1. 调整均匀采样参数
2. 调整录取人数过滤阈值
3. 调整弹窗文字
