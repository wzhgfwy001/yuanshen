# SESSION-STATE.md - 当前会话关键状态

> ⚠️ **已迁移**：本文件关键内容已并入 `brain/progress.json`
> 新增状态请优先写入 `brain/progress.json`

---

## 当前会话

| 字段 | 值 |
|------|-----|
| 会话开始时间 | 2026-04-08 |
| 商户号(mch_id) | 1743894878 |
| 当前任务 | **小程序变现 + 小说上架番茄** |
| 用户目标 | 财富自由 |

## ⚠️ 用户偏好（必须遵守）

| 偏好 | 说明 |
|------|-----|
| **Dreaming模式** | 不主动启用，等用户明确要求才启动 |

## 🔴 开发规范（强制执行）

| 规则 | 说明 |
|------|-----|
| **默认使用多Agent系统** | 复杂任务一律启用多Agent协作 |
| **并行处理** | 多个独立任务同时分配给子Agent |
| **审查机制** | 每个任务完成后必须有质量检查 |

## 📋 产品规则摘要

### 收费模式
- 本科¥99.9/专科¥79.9/复购¥55.9
- 分享解锁免费1次（50条）

### 结果呈现
- 100条，冲稳保分布
- 未缴费预览各3条

### 微信支付
- 商户号：1743894878
- AppID：wx21c2c6114d560057

---

_最后更新：2026-04-22_

## 🚨 重要变更 (05:31)

**2026-04-22 向量数据库已卸载：**

- **D:/vector_db/** 目录已删除
- **ChromaDB** 已从 Python 环境卸载
- **LM Studio** 进程已关闭
- **memory_search** 降级为纯文本搜索
- **内存状态：** 从 2.7GB 可用 → 9.4GB 可用

**影响范围：**
- `writeAndVectorize()` → 失效（向量写入）
- `deleteAndRemoveVector()` → 失效（向量删除）
- `memory_search` → 纯文本搜索模式
- HEARTBEAT.md 第11项（自动向量化检查）→ 已移除

---

## 🚨 用户纠正 (01:53) - 已过期

**问题：** 向量数据库双向同步已实现，但元神"失忆"又问用户是否需要实现

**状态：** 此问题已于 2026-04-22 解决（通过卸载向量数据库）

---

## 🚨 教训 (17:54)

**变更时的影响范围检查清单：**
当某个底层组件被删除/替换时，必须同步检查：
1. 配置文件（.json）
2. 硬编码路径（.js / .py）
3. 脚本文件（.bat / .py / .sh）
4. 文档引用（.md）
5. 文档章节（AGENTS.md 等）

**本次教训：** 2026-04-22 删除 LM Studio + ChromaDB 时，只清理了 openclaw.json，漏掉了 skills/ 目录下 8 个硬编码 D:/vector_db 的 JS 文件，导致后续调用时系统崩溃。

---

## 🚨 上下文守卫配置

**触发规则：**
- 60%：记录，不行动
- 80%：清理working-buffer + 保存记忆
- 90%：强制压缩 + 通知用户

**执行周期：** 每30分钟
